C     -*- fortran -*-
C     This file is autogenerated with f2py (version:2)
C     It contains Fortran 77 wrappers to fortran functions.

      subroutine f2pyinitintlis(setupfunc)
      external setupfunc
      integer indupd
      integer nstupd
      integer nintpr
      integer mxipot
      integer icntud
      integer nuptdl(10000)
      common /intlis/ indupd,nstupd,nintpr,mxipot,icntud,nuptdl
      call setupfunc(indupd,nstupd,nintpr,mxipot,icntud,nuptdl)
      end

      subroutine f2pyinitneighborlist(setupfunc)
      external setupfunc
      integer initflag
      integer nncount
      real*8 rold(15000)
      real*8 dr
      real*8 dr1
      real*8 dr2
      real*8 rij(3)
      real*8 rxyz(3)
      real*8 r2pr(200000)
      real*8 delpr(600000)
      real*8 r2st(5000)
      real*8 rhoij(200000)
      real*8 rhofij(600000)
      real*8 rhofji(600000)
      real*8 rhoji(200000)
      real*8 potpr(200000)
      real*8 drhoijr(200000)
      real*8 drhojir(200000)
      integer indpra(2,200000)
      integer iptpr1(5000,3)
      integer iptcom(4)
      integer nintst(3)
      integer natm1(3)
      integer natm2(3)
      integer nintp(3)
      common /neighborlist/ initflag,nncount,rold,dr,dr1,dr2,rij,r
     &xyz,r2pr,delpr,r2st,rhoij,rhofij,rhofji,rhoji,potpr,drhoijr,drhoji
     &r,indpra,iptpr1,iptcom,nintst,natm1,natm2,nintp
      call setupfunc(initflag,nncount,rold,dr,dr1,dr2,rij,rxyz,r2p
     &r,delpr,r2st,rhoij,rhofij,rhofji,rhoji,potpr,drhoijr,drhojir,indpr
     &a,iptpr1,iptcom,nintst,natm1,natm2,nintp)
      end

      subroutine f2pyinitthings(setupfunc)
      external setupfunc
      integer isame(3)
      integer ifisame(3)
      integer nshift(3)
      common /things/ isame,ifisame,nshift
      call setupfunc(isame,ifisame,nshift)
      end

      subroutine f2pyinitbaths(setupfunc)
      external setupfunc
      real*8 crate
      real*8 temmas
      real*8 pextl
      real*8 volapp
      real*8 plambd
      real*8 ratlinch
      real*8 colrat
      logical incol
      logical imasv
      logical irate
      integer nmasv
      real*8 textl
      logical ipres
      logical ipcol
      logical ilinch
      logical iqkmin
      logical ispf
      integer indpara
      common /baths/ crate,temmas,pextl,volapp,plambd,ratlinch,col
     &rat,incol,imasv,irate,nmasv,textl,ipres,ipcol,ilinch,iqkmin,ispf,i
     &ndpara
      call setupfunc(crate,temmas,pextl,volapp,plambd,ratlinch,col
     &rat,incol,imasv,irate,nmasv,textl,ipres,ipcol,ilinch,iqkmin,ispf,i
     &ndpara)
      end

      subroutine f2pyinitnudge(setupfunc)
      external setupfunc
      real*8 cosphismall
      real*8 cosphilarge
      real*8 tolforce
      logical inudge
      logical iqkmconv
      common /nudge/ cosphismall,cosphilarge,tolforce,inudge,iqkmc
     &onv
      call setupfunc(cosphismall,cosphilarge,tolforce,inudge,iqkmc
     &onv)
      end

      subroutine f2pyinitgeom(setupfunc)
      external setupfunc
      real*8 ax
      real*8 ay
      real*8 az
      real*8 ax0
      real*8 ay0
      real*8 az0
      real*8 alpha
      real*8 beta
      real*8 gamma
      real*8 sinalp
      real*8 cosalp
      real*8 sinbet
      real*8 cosbet
      real*8 singam
      real*8 cosgam
      real*8 zsurf
      real*8 zbott
      integer indzbott
      integer indzsurf
      common /geom/ ax,ay,az,ax0,ay0,az0,alpha,beta,gamma,sinalp,c
     &osalp,sinbet,cosbet,singam,cosgam,zsurf,zbott,indzbott,indzsurf
      call setupfunc(ax,ay,az,ax0,ay0,az0,alpha,beta,gamma,sinalp,
     &cosalp,sinbet,cosbet,singam,cosgam,zsurf,zbott,indzbott,indzsurf)
      end

      subroutine f2pyinitconf1(setupfunc)
      external setupfunc
      real*8 ra(15000)
      real*8 pmass
      real*8 volm
      real*8 amass(10)
      real*8 avera(15000)
      real*8 sumra(15000)
      real*8 ranopbc(15000)
      common /conf1/ ra,pmass,volm,amass,avera,sumra,ranopbc
      call setupfunc(ra,pmass,volm,amass,avera,sumra,ranopbc)
      end

      subroutine f2pyinitconf2(setupfunc)
      external setupfunc
      real*8 va(15000)
      real*8 fa(15000)
      real*8 volv
      real*8 potpat(5000)
      real*8 djnudge(15000)
      real*8 djnnudge(15000)
      real*8 fuperpt2(15000)
      real*8 fat2(15000)
      integer indupdproj
      common /conf2/ va,fa,volv,potpat,djnudge,djnnudge,fuperpt2,f
     &at2,indupdproj
      call setupfunc(va,fa,volv,potpat,djnudge,djnnudge,fuperpt2,f
     &at2,indupdproj)
      end

      subroutine f2pyinitconf3(setupfunc)
      external setupfunc
      integer itag(5000)
      integer natoms
      integer natype
      integer natms(10)
      common /conf3/ itag,natoms,natype,natms
      call setupfunc(itag,natoms,natype,natms)
      end

      subroutine f2pyinitpermafrost(setupfunc)
      external setupfunc
      real*8 rinfraperm(15000)
      integer ninfraperm
      integer nainfraperm
      integer naperm
      integer iperm(5000)
      integer itinfraperm(5000)
      common /permafrost/ rinfraperm,ninfraperm,nainfraperm,naperm
     &,iperm,itinfraperm
      call setupfunc(rinfraperm,ninfraperm,nainfraperm,naperm,iper
     &m,itinfraperm)
      end

      subroutine f2pyinitharmcntr(setupfunc)
      external setupfunc
      real*8 harmcntr(15000)
      real*8 harmspr(5000)
      integer nharm
      integer iharm(5000)
      common /harmcntr/ harmcntr,harmspr,nharm,iharm
      call setupfunc(harmcntr,harmspr,nharm,iharm)
      end

      subroutine f2pyinitfpi(setupfunc)
      external setupfunc
      real*8 sprconfpi(1200)
      real*8 enerfpi(1200)
      real*8 cntrfa(1200)
      integer nfpi
      integer nfpistr
      integer nfpicir
      integer nfpicnt
      integer nfpicom(10)
      integer itagfpi(1200)
      integer numimage
      integer ifpi(1200)
      integer nimfpi(1200)
      integer numfpi(1200)
      integer naconstr
      integer iconstr(5000)
      integer indallinchains
      integer indallfixend
      integer ifirsttag
      common /fpi/ sprconfpi,enerfpi,cntrfa,nfpi,nfpistr,nfpicir,n
     &fpicnt,nfpicom,itagfpi,numimage,ifpi,nimfpi,numfpi,naconstr,iconst
     &r,indallinchains,indallfixend,ifirsttag
      call setupfunc(sprconfpi,enerfpi,cntrfa,nfpi,nfpistr,nfpicir
     &,nfpicnt,nfpicom,itagfpi,numimage,ifpi,nimfpi,numfpi,naconstr,icon
     &str,indallinchains,indallfixend,ifirsttag)
      end

      subroutine f2pyinitconf4(setupfunc)
      external setupfunc
      character cphead(10,80,1)
      common /conf4/ cphead
      call setupfunc(cphead)
      end

      subroutine f2pyinitlangevin(setupfunc)
      external setupfunc
      integer ilangevin(15000)
      integer nlangevin
      common /langevin/ ilangevin,nlangevin
      call setupfunc(ilangevin,nlangevin)
      end

      subroutine f2pyinitenergy(setupfunc)
      external setupfunc
      real*8 ttot
      real*8 tt(6,10)
      real*8 utot
      real*8 ut(55)
      real*8 virtot
      real*8 virial(55)
      real*8 virmat(6,55)
      real*8 virvec
      common /energy/ ttot,tt,utot,ut,virtot,virial,virmat,virvec
      call setupfunc(ttot,tt,utot,ut,virtot,virial,virmat,virvec)
      end

      subroutine f2pyinitenperat(setupfunc)
      external setupfunc
      real*8 potenpat(5000)
      real*8 eldensity(5000,3)
      integer iatshift1
      integer iatshift2
      common /enperat/ potenpat,eldensity,iatshift1,iatshift2
      call setupfunc(potenpat,eldensity,iatshift1,iatshift2)
      end

      subroutine f2pyinitpotent(setupfunc)
      external setupfunc
      real*8 potpar(100,3)
      real*8 rskinmax
      real*8 rskin(3)
      real*8 rskin2(3)
      real*8 skndpt(3)
      real*8 rcut(3)
      real*8 rcut2(3)
      real*8 rhocut1st(10)
      real*8 rhocut2st(10)
      real*8 phicutst(10)
      integer maxneb
      integer maxnpr
      integer npotpar
      integer iorderf
      integer indf1
      common /potent/ potpar,rskinmax,rskin,rskin2,skndpt,rcut,rcu
     &t2,rhocut1st,rhocut2st,phicutst,maxneb,maxnpr,npotpar,iorderf,indf
     &1
      call setupfunc(potpar,rskinmax,rskin,rskin2,skndpt,rcut,rcut
     &2,rhocut1st,rhocut2st,phicutst,maxneb,maxnpr,npotpar,iorderf,indf1
     &)
      end

      subroutine f2pyinitlunits(setupfunc)
      external setupfunc
      integer lunout
      integer luninp
      integer lunci
      integer lunco
      integer luncm
      integer lunpot
      integer lunins
      integer luntgr
      integer lunaco
      integer lunvis
      integer lunattr
      integer luncofpi
      integer lunoutfpi
      integer lunqckmin
      integer lunmaxim
      integer luncish
      common /lunits/ lunout,luninp,lunci,lunco,luncm,lunpot,lunin
     &s,luntgr,lunaco,lunvis,lunattr,luncofpi,lunoutfpi,lunqckmin,lunmax
     &im,luncish
      call setupfunc(lunout,luninp,lunci,lunco,luncm,lunpot,lunins
     &,luntgr,lunaco,lunvis,lunattr,luncofpi,lunoutfpi,lunqckmin,lunmaxi
     &m,luncish)
      end

      subroutine f2pyinitunscale(setupfunc)
      external setupfunc
      real*8 ralocal(15000)
      common /unscale/ ralocal
      call setupfunc(ralocal)
      end


